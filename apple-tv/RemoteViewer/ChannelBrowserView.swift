import SwiftUI

/// Channel browser — not used in v1 (player handles channel switching directly).
/// Kept as a placeholder for future use.
struct ChannelBrowserView: View {
    var body: some View {
        Text("Channel Browser")
    }
}
