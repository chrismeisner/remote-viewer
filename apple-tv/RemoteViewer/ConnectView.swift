import SwiftUI

/// Placeholder — the v1 app auto-connects, so this view is not currently used.
struct ConnectView: View {
    var body: some View {
        ProgressView("Connecting\u{2026}")
    }
}
