# Media root for schedule files and playback scanning
MEDIA_ROOT=/absolute/path/to/media

# Optional: override ffprobe path if not on PATH
FFPROBE_PATH=/usr/local/bin/ffprobe

# Remote media manifest and FTP upload
REMOTE_MEDIA_BASE=https://chrismeisner.com/media/
FTP_HOST=your-host
FTP_PORT=21
FTP_USER=your-user
FTP_PASS=your-pass
FTP_REMOTE_PATH=/public_html/media/media-index.json
FTP_SECURE=false

